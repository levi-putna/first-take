import { AbsoluteFill } from "@storyboard/core";

/**
 * Lead-in bumper for audio-mix (solid dark teal).
 */
export default function Lead() {
  return <AbsoluteFill style={{ backgroundColor: "#0a3d3d" }} />;
}
